// This code does not execute pro perly. Try to figure out why. //

//BUG//
function makeNegative(num) {
    // Code?
  }

  //DEBUG// 
  function makeNegative(num) {
    if (num > 0) {
      return -num;
    } else {
      return num;
    }
  }