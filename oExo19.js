/*Écrivez une fonction qui convertit la chaîne d'entrée en majuscules.*/

//BUG//
function makeUpperCase(str) {
    // Code here
  }
//DEBUG//
function makeUpperCase(str) {
    return str.toUpperCase();
  }